# 流程控制语句

程序并不是简单的“从上到下”执行，很多时候，我们希望程序能根据不同的情况，做出不同的选择，比如：根据情况跳过某些代码，或者重复执行某些代码，那这时就需要用到『流程控制语句』，程序的执行流程大体上可分为三类：**顺序**、**分支**、**循环**。

**备注**：其中顺序执行是最简单的，就是按照程序的编写顺序依次执行，所以我们不再探讨顺序执行。

## 1. 分支

- 分支有很多其他的称呼，比如：条件控制语句、分支语句、选择语句。
- 分之是通过条件判断，来决定执行哪些代码。

### 1.1. 单分支

**语法格式：**

```
if 判断条件:
    条件【成立】时执行的代码1
    条件【成立】时执行的代码2
    ......
```

**注意：**Python 靠代码缩进来识别代码范围，所以条件成立时要执行的代码前，必须加空格。

**示例代码：**

```python
age = int(input('请输入你的年龄：'))
if age >= 18:
    print('你是成年人')
    print('成年人的世界，虽不容易，但很精彩！')
print('欢迎你来学习Python！')
```

**石头剪刀布：简易版**

```python
user_input = input("1.石头\n2.剪刀\n3.布\n请输入你的选择：")
bot_input = "1"
if user_input == "1":
    print("打平了")

if user_input == "2":
    print("你输了")

if user_input == "3":
    print('你赢了！')
```





### 1.2. 双分支

**语法格式：**

```
if 判断条件:
    条件【成立】时执行的代码1
    条件【成立】时执行的代码2
else:
    条件成【不成立】时执行的代码1
    条件成【不成立】时执行的代码2
```

**示例代码：**

```python
age = int(input('请输入你的年龄：'))
if age >= 18:
    print('你是成年人')
    print('成年人的世界，虽不容易，但很精彩！')
else:
    print('你是未成年人')
    print('好好加油，努力学习，未来可期！')
print('欢迎你来学习Python！')
```

### 1.3. 多分支

**语法格式：**

```
if 判断条件1:
    条件1【成立】时执行的代码
elif 判断条件2:
    条件2【成立】时执行的代码
elif 判断条件3:
    条件3【成立】时执行的代码
else:  # else如不需要可以省略
    上述所有条件都不成立时执行的代码
```

**示例代码：**

```python
# 根据年龄来判断处于人生哪个阶段。
age = int(input('请输入你的年龄：'))
if age <= 10:
    print('你是幼儿')
elif age <= 18:
    print('你是青少年')
elif age <= 30:
    print('你是青年')
elif age <= 50:
    print('你是中年')
elif age <= 60:
    print('你是中老年')
else:
    print('你是老年')
```

使用多分支语句时，需要注意下面几点：

> 1. 一个`if`语句只能匹配`1`个`else`语句，但可以匹配多个`elif`语句，并且`else`语句要在所有的`elif`语句之后。
> 2. 一旦某个分支语句检测为`true`，其他的`elif`以及`else`语句都将不再执行。

### 1.4. 嵌套分支

**语法格式：**

```python
if 判断条件1:
    # 条件1 成立时执行的代码1
    # 条件1 成立时执行的代码2
    # ......
    if 判断条件2:
        # 条件2 成立时执行的代码1
        # 条件2 成立时执行的代码2
        # ......
    elif 判断条件3:
        # 条件3 成立时执行的代码
        # ......
    else:
        # 条件2、条件3 都不成立时执行的代码1
        # 条件2、条件3 都不成立时执行的代码2
        # ......
else:
    # 条件1 不成立时执行的代码1
    # 条件1 不成立时执行的代码2
    # ......
    if 判断条件4:
        # 条件4 成立时执行的代码
        # ......
    else:
        # 条件4 不成立时执行的代码
        # ......
```



**升级版石头剪刀布**

```python
import random
user_input = input("1.石头\n2.剪刀\n3.布\n请输入你的选择：")
options = ['1','2','3']
bot_input = random.choice(options)
# print(f'bot:{bot_input}')
state = 0 # 1.平 2.赢 3.输
if user_input == "1": # 用户石头
    if bot_input == "1":
        state = 1
    elif bot_input == "2":
        state = 2
    else:
        state = 3
elif user_input == '2': # 用户剪刀
    if bot_input == '1':
        state = 3
    elif bot_input == '2':
        state = 1
    else:
        state = 2
elif user_input == '3': # 用户：布
    if bot_input == '1':
        state = 2
    elif bot_input == '2':
        state = 3
    else:
        state = 1

if state == 1:
    print("半斤八两")
elif state == 2:
    print("you win")
elif state == 3:
    print("you lose")
else:
    print("用户输入有误，请重来。")
```





```python
age = int(input('请输入你的年龄：'))
has_report = input('您是否提交了体检报告？（是/否）')
level = int(input('请输入你的会员等级（1/2/3）'))

print('******⬇️程序的识别结果如下⬇️：******')
if 18 <= age <= 45:
    print('✅️您的年龄符合比赛要求！')
    if has_report == '是':
        print('✅️您已提交体检报告！')
        print('✅️您可以参加比赛！')
        if level == 1:
            print(f'😊尊敬的{level}会员，比赛结束后，您可以领取纪念T恤👕一件！')
        elif level == 2:
            print(f'😊尊敬的{level}会员，比赛结束后，您可以领取专业跑鞋👟一双！')
        elif level == 3:
            print(f'😊尊敬的{level}会员，比赛结束后，您可以领取运动耳机🎧️一副！')
        else:
            print('❌您输入的会员等级不正确！')
    elif has_report == '否':
        print('❌您未提交体检报告，不能参加比赛！')
    else:
        print('❌您输入的体检报告有误！')
else:
    print('❌抱歉，参赛年龄需要在18~45之间！')
```



### 课堂练习

🎮 作业：冒险者公会任务系统

背景故事：你来到了冒险者公会，想要接取任务赚取金币。公会接待员会根据你的等级和职业来判断你能接取什么难度的任务。

作业要求：
1. 输入玩家信息（名字、等级1-50、职业：战士/法师/弓箭手）
2. 根据等级判断可接任务（if-elif-else）：
   - 等级<10：新手村除草（10金币）
   - 10-19级：清理史莱姆（30金币）
   - 20-29级：讨伐哥布林（80金币）
   - 30-39级：巨龙讨伐（200金币）
   - ≥40级：魔王城决战（500金币）
3. 特殊规则：
   - 战士且等级≥25：额外解锁竞技场冠军（150金币）
   - 等级<5：提示你太弱了，先去新手村练级吧！
4. 按指定格式输出冒险者档案和可接任务列表

考察：input、int转换、if-elif-else、逻辑运算符and、字符串格式化


